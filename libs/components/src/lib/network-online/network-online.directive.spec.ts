import { NetworkOnlineDirective } from './network-online.directive';

describe('NetworkOnlineDirective', () => {
  it('should create an instance', () => {
    const directive = new NetworkOnlineDirective();
    expect(directive).toBeTruthy();
  });
});
