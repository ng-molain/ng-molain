import { FormProviderDirective } from '../form-provider.directive';

describe('FormProviderDirective', () => {
  it('should create an instance', () => {
    const directive = new FormProviderDirective();
    expect(directive).toBeTruthy();
  });
});
